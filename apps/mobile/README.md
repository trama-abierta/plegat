# App móvil

Reservada para una futura aplicación React Native + Expo en JavaScript. Todavía no contiene una app funcional.

Deberá reutilizar los contratos HTTP, las reglas de transición e idempotencia y OAuth/PKCE. El modo offline requiere reconciliación explícita; nunca debe inventar fichajes confirmados.
