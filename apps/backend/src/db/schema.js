import { pgTable, text, uuid, integer, timestamp, jsonb, unique } from 'drizzle-orm/pg-core';
export const tenants = pgTable('tenants', { id: text('id').primaryKey(), name: text('name').notNull(), slug: text('slug').notNull(), timeZone: text('time_zone').notNull(), status: text('status').notNull(), createdAt: timestamp('created_at', { withTimezone: true }).notNull() });
export const tenantSettings = pgTable('tenant_settings', { tenantId: text('tenant_id').primaryKey().references(() => tenants.id), settings: jsonb('settings').notNull(), updatedBy: text('updated_by').references(() => users.id), updatedAt: timestamp('updated_at', { withTimezone: true }).notNull() });
export const users = pgTable('users', { id: text('id').primaryKey(), email: text('email').notNull().unique(), name: text('name').notNull(), passwordHash: text('password_hash').notNull(), platformRole: text('platform_role'), status: text('status').notNull() });
export const memberships = pgTable('memberships', { userId: text('user_id').notNull().references(() => users.id), tenantId: text('tenant_id').notNull().references(() => tenants.id), role: text('role').notNull(), status: text('status').notNull() }, table => ({ pk: unique().on(table.userId, table.tenantId) }));
export const sessions = pgTable('sessions', { id: text('id').primaryKey(), userId: text('user_id').notNull().references(() => users.id), activeTenantId: text('active_tenant_id').references(() => tenants.id), expiresAt: timestamp('expires_at', { withTimezone: true }).notNull(), createdAt: timestamp('created_at', { withTimezone: true }).notNull(), lastSeenAt: timestamp('last_seen_at', { withTimezone: true }).notNull() });
export const oauthClients = pgTable('oauth_clients', { id: text('id').primaryKey(), name: text('name').notNull(), type: text('type').notNull(), redirectUris: jsonb('redirect_uris').notNull(), allowedScopes: jsonb('allowed_scopes').notNull(), status: text('status').notNull() });
export const externalIdentities = pgTable('external_identities', { id: text('id').primaryKey(), userId: text('user_id').notNull().references(() => users.id), provider: text('provider').notNull(), providerSubject: text('provider_subject').notNull(), email: text('email').notNull(), emailVerified: text('email_verified').notNull(), profileSnapshot: jsonb('profile_snapshot').notNull(), createdAt: timestamp('created_at', { withTimezone: true }).notNull(), lastLoginAt: timestamp('last_login_at', { withTimezone: true }).notNull() }, table => ({ providerSubjectUnique: unique().on(table.provider, table.providerSubject) }));
export const oauthAuthorizationCodes = pgTable('oauth_authorization_codes', { id: text('id').primaryKey(), clientId: text('client_id').notNull().references(() => oauthClients.id), userId: text('user_id').notNull().references(() => users.id), redirectUri: text('redirect_uri').notNull(), scope: text('scope').notNull(), codeChallenge: text('code_challenge').notNull(), codeChallengeMethod: text('code_challenge_method').notNull(), stateHash: text('state_hash'), expiresAt: timestamp('expires_at', { withTimezone: true }).notNull(), usedAt: timestamp('used_at', { withTimezone: true }) });
export const desktopSessions = pgTable('desktop_sessions', { id: text('id').primaryKey(), userId: text('user_id').notNull().references(() => users.id), clientId: text('client_id').notNull().references(() => oauthClients.id), activeTenantId: text('active_tenant_id').references(() => tenants.id), accessTokenHash: text('access_token_hash').notNull().unique(), refreshTokenHash: text('refresh_token_hash').notNull().unique(), deviceName: text('device_name'), platform: text('platform'), appVersion: text('app_version'), accessExpiresAt: timestamp('access_expires_at', { withTimezone: true }).notNull(), expiresAt: timestamp('expires_at', { withTimezone: true }).notNull(), createdAt: timestamp('created_at', { withTimezone: true }).notNull(), lastSeenAt: timestamp('last_seen_at', { withTimezone: true }).notNull(), revokedAt: timestamp('revoked_at', { withTimezone: true }) });
export const employees = pgTable('employees', { id: text('id').primaryKey(), userId: text('user_id').references(() => users.id), tenantId: text('tenant_id').notNull().references(() => tenants.id), name: text('name').notNull(), email: text('email').notNull() });
export const attendanceEvents = pgTable('attendance_events', { id: uuid('id').defaultRandom().primaryKey(), employeeId: text('employee_id').notNull().references(() => employees.id), type: text('type').notNull(), occurredAt: timestamp('occurred_at', { withTimezone: true }).notNull(), sequence: integer('sequence').notNull(), idempotencyKey: text('idempotency_key').notNull() });
export const attendanceStates = pgTable('attendance_states', { employeeId: text('employee_id').primaryKey().references(() => employees.id), status: text('status').notNull(), revision: integer('revision').notNull(), updatedAt: timestamp('updated_at', { withTimezone: true }).notNull() });
export const attendanceChangeRequests = pgTable('attendance_change_requests', { id: uuid('id').defaultRandom().primaryKey(), tenantId: text('tenant_id').notNull().references(() => tenants.id), employeeId: text('employee_id').notNull().references(() => employees.id), requestedBy: text('requested_by').notNull().references(() => users.id), kind: text('kind').notNull(), eventId: uuid('event_id').references(() => attendanceEvents.id), originalTime: timestamp('original_time', { withTimezone: true }), proposedTime: timestamp('proposed_time', { withTimezone: true }), reason: text('reason').notNull(), proposal: jsonb('proposal').notNull(), status: text('status').notNull(), reviewedBy: text('reviewed_by').references(() => users.id), reviewedAt: timestamp('reviewed_at', { withTimezone: true }), reviewComment: text('review_comment'), createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow() });
export const attendanceWorkdayProjections = pgTable('attendance_workday_projections', { id: uuid('id').defaultRandom().primaryKey(), tenantId: text('tenant_id').notNull().references(() => tenants.id), employeeId: text('employee_id').notNull().references(() => employees.id), workDate: timestamp('work_date', { mode: 'date' }).notNull(), sourceRequestId: uuid('source_request_id').notNull().references(() => attendanceChangeRequests.id), status: text('status').notNull() });
export const attendanceProjectionEvents = pgTable('attendance_projection_events', { projectionId: uuid('projection_id').notNull().references(() => attendanceWorkdayProjections.id), position: integer('position').notNull(), type: text('type').notNull(), occurredAt: timestamp('occurred_at', { withTimezone: true }).notNull(), sourceEventId: uuid('source_event_id') });
export const auditLog = pgTable('audit_log', { id: uuid('id').defaultRandom().primaryKey(), actorUserId: text('actor_user_id').references(() => users.id), tenantId: text('tenant_id').references(() => tenants.id), action: text('action').notNull(), targetType: text('target_type'), targetId: text('target_id'), metadata: jsonb('metadata').notNull(), occurredAt: timestamp('occurred_at', { withTimezone: true }).notNull() });
